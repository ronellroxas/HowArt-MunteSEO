# CCINOV8 Prototype

**Run both backend and frontend:** run `npm run dev-all`

**Run frontend only:**
1. go to root folder (*howard-munteseo*)
2. run `npm run dev`

**Run backend only:**
1. go to root folder 
2. `cd backend`
3. run `npm run dev`
